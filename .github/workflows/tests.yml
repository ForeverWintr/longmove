# This mostly comes from examples here: https://github.com/marketplace/actions/install-poetry-action

name: Lint and Test
on:
  push:
    branches: [ main ]
  pull_request:


jobs:

  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: j178/prek-action@v1


  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.13", "3.14"]
        os: ["ubuntu-latest", "macos-latest"] # Not all python versions are available for all OSes
    defaults:
      run:
        shell: bash
    steps:
    - uses: actions/checkout@v6

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
        # cache: 'poetry' # Needs poetry lockfile.

    - name: Install poetry
      uses: snok/install-poetry@v1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true
        # installer-parallel: true # May have caused an issue on macos

    #----------------------------------------------
    #       load cached venv if cache exists
    #----------------------------------------------
    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v4
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}

    #----------------------------------------------
    # install dependencies if cache does not exist
    #----------------------------------------------
    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      run: poetry install --no-interaction --no-root

    - name: Install library
      run: poetry install --no-interaction

    # macOS ships Apple's openrsync (no --info/progress support). Install GNU
    # rsync and put it ahead of /usr/bin on PATH so the transfer tests run.
    - name: Install GNU rsync (macOS)
      if: runner.os == 'macOS'
      env:
        HOMEBREW_NO_AUTO_UPDATE: "1"
      run: |
        brew install rsync
        echo "$(brew --prefix)/bin" >> "$GITHUB_PATH"
        "$(brew --prefix)/bin/rsync" --version | head -n 1

    - name: Run tests
      run: poetry run pytest --cov=longmove --cov-report=xml


    #----------------------------------------------
    #             upload coverage stats
    # (requires CODECOV_TOKEN in repository secrets)
    #----------------------------------------------
    - name: Upload coverage
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        file: ./coverage.xml
        fail_ci_if_error: true
